import React from "react"
import { useNavigate } from "react-router-dom";

  const Profile=()=>{
      const navigate = useNavigate();
    return(
        <div>
Hi loved ones its a Home  page<button>Change to about page</button>
        </div>
    )

    
}
export default Profile